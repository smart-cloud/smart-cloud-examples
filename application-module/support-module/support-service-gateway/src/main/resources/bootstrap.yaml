spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        extensionConfigs:
        - dataId: gateway-route.yaml
          refresh: true
        - dataId: gateway-redis.yaml
          refresh: true
